/* import React from 'react'

export default function ListItem({value, idx, onDelete}) {
    return (
        <li >
            <span style={{textDecoration: 'line-through'}} />{value}</span> 
            <button>Done</button>
            <button onClick={() => onDelete(idx)} style={{backgroundColor: 'red'}} >Delete</button>
        </li>
    )
}
 */